# Незавершені задачі

Оновлено: 2026-09-12. Нижче — актуальні рекомендації після реалізації reliability foundation у draft PR #2; це не автоматично затверджена дорожня карта.

| ID / пріоритет | Задача | Статус / залежності | Критерій завершення |
|---|---|---|---|
| T02 / P2 | Повернути видимий доступ до «Джерело» на desktop | Рекомендація; навігація | Діалог доступний без прихованого sidebar |
| T03 / P2 | Пояснювати частковий розподіл, коли деякі значення null | Рекомендація; DATA_MODEL | Видно, що відсотки рахуються за наявними даними; null не замінено нулем |
| T05 / P3 | Прибрати мертві CSS-селектори й упорядкувати каскад | Етап 3; потрібна regression перевірка layout | Поведінка одного ряду збережена без spacer/клонів і візуальних регресій |
| T08 / P3 | Профілювання великої книги, Safari/Firefox і фізичні пристрої | Етап 3/4; потрібні середовища/тестова книга | Є виміряні результати й зафіксовані межі, а не декларація сумісності |
| T11 / P2 | Додати кеш/індекси для повторних aggregate | Етап 3; бажано спершу профілювання | Повторний render не перераховує однакові зрізи без потреби |
| T12 / P2 | Поступово декомпозувати `contour.js` без framework rewrite | Етап 3 | Відповідальності розділені без зміни контрактів UI |
| T15 / P2 | Прибрати dead DOM старого sidebar/donut після перевірки залежностей | Етап 3 | Неактивні вузли/handlers вилучені без responsive-регресій |
| T16 / P3 | Уніфікувати cache revision/resource versioning | Після стабілізації Stage 2; не потребує build-system | Версія активних ресурсів змінюється з одного місця або через простий контрольований механізм |
| T13 / P2 | Додати GitHub Actions для автоматичних adapter/syntax перевірок | Етап 4 | Pull request отримує автоматичний pass/fail status |
| T14 / P2 | Додати browser smoke/regression tests | Етап 4 | Критичні сценарії запуску/імпорту/навігації/графіків перевіряються автоматично |

### Очікує перевірки перед merge Stage 2

- `node --check js/contour-config.js`
- `node --check js/contour-data.js`
- `node --check js/contour.js`
- `node .audit/verify-contour.cjs`
- browser smoke: bundled/user source, schema error/warning, ручні дати, швидкі перемикання ApexCharts і detail chart.

Закрито кодом у Stage 2: T04 (видимий chart error-state + generation guard), T07 (централізована включна date policy), T09 (централізований Excel contract + validation), T10 (`source.kind` замість filename-based classification). Остаточно вважати Stage 2 завершеним після regression/browser перевірки та merge PR #2.

Закрито в Stage 1: переносимий `Запустити.cmd`, GitHub як канонічне сховище, `.gitignore/.gitattributes`, вилучення неактивних історичних MVC/Dexie/Bootstrap/ручних версій з активного дерева.

Невідомі одиниці площі та помилки вартості потребують підтвердженого джерела, а не припущень у коді.
